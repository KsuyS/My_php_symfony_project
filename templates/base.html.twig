<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">

		{% block stylesheets %}
			<style>
				body {
					font-family: 'Arial, sans-serif';
					background-color: #f0f4ff;
					color: #3a3f58;
					padding: 30px;
					max-width: 700px;
					margin: 0 auto;
				}

				h1 {
					color: #4a5ab8;
					border-bottom: 2px solid #4a5ab8;
					padding-bottom: 10px;
					margin-bottom: 30px;
				}

				.user-item {
					background-color: #ffffff;
					border: 1px solid #c5d0e6;
					border-radius: 8px;
					padding: 15px 20px;
					margin-bottom: 15px;
					display: flex;
					align-items: center;
					justify-content: space-between;
					box-shadow: 0 2px 6px rgba(74, 90, 184, 0.1);
					transition: box-shadow 0.3s ease;
				}

				.user-item:hover {
					box-shadow: 0 4px 12px rgba(74, 90, 184, 0.25);
				}

				.user-link {
					font-size: 20px;
					color: #2e3a87;
					text-decoration: none;
					font-weight: 600;
				}

				.user-link:hover {
					text-decoration: underline;
				}

				.user-info {
					margin-bottom: 15px;
					font-size: 18px;
				}

				img {
					margin-top: 10px;
					border-radius: 8px;
					box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
					max-width: 200px;
					height: auto;
					display: block;
				}

				a {
					text-decoration: none;
				}
                
				label {
					display: block;
					margin-bottom: 6px;
					font-weight: bold;
					color: #2e3a87;
				}

				button {
					background-color: #4a5ab8;
					color: white;
					border: none;
					padding: 10px 20px;
					font-size: 16px;
					border-radius: 6px;
					cursor: pointer;
					transition: background-color 0.3s ease;
				}

				button[type="submit"] {
					background-color: #4a5ab8;
					width: 100%;
					color: white;
					border: none;
					padding: 10px 20px;
					font-size: 16px;
					border-radius: 6px;
					cursor: pointer;
					transition: background-color 0.3s ease;
				}

				input[type="text"],
				input[type="date"],
				input[type="email"],
				input[type="tel"] {
					width: 100%;
					padding: 8px 10px;
					border: 1px solid #ccc;
					border-radius: 4px;
					box-sizing: border-box;
					font-size: 14px;
					transition: border-color 0.3s ease;
				}

				form {
					background-color: #fff;
					max-width: 500px;
					padding: 25px 30px;
					border-radius: 8px;
					box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
				}

				form div {
					margin-bottom: 15px;
				}
			</style>
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body style="font-size: 20px"> {% block body %}{% endblock %}
		</body>
	</html>
